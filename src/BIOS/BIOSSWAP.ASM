; One complete prompt owner: cold source is outside the retained low boundary.
.8086
INCLUDE MSEQU.INC
INCLUDE DEVSYM.INC
IFDEF BIOS_SWAP_HIGH
BIOSHIGH SEGMENT BYTE PUBLIC 'BIOSHIGH'
ASSUME CS:BIOSHIGH
EXTRN BIOS_SERVICE_LOW_SEGMENT:WORD,BIOS_CHAR_INT16:DWORD
EXTRN BIOS_SWAP_OUTCHR_OFFSET:WORD,BIOS_SERVICE_VECTOR_GATE:DWORD
ELSE
INCLUDE MSGROUP.INC
EXTRN OUTCHR:NEAR
ENDIF
EXTRN FLUSH:NEAR
PUBLIC BIOS_SWAP_BODY_START,BIOS_SWAP_BODY_END
BIOS_SWAP_BODY_START LABEL BYTE
INCLUDE SWAPPROM.INC
BIOS_SWAP_BODY_END LABEL BYTE
IFDEF BIOS_SWAP_HIGH
BIOSHIGH ENDS
ELSE
CODE ENDS
ENDIF
END
